# RAG 系统待办事项

## 最近改动（2026-08-20）

- 意图路由 → `deepseek-ai/DeepSeek-R1-0528-Qwen3-8B`；视觉 → `deepseek-ai/DeepSeek-OCR`（见 docs/plans/2026-08-20-intent-vision-model-switch.md）
- 待实测：真实 API 验证 + 复跑评测

## 高优先级（影响核心功能）

### 1. H类（错误前提纠偏）改进
**当前状态**：1.40/3（60% → 53.3%，反而下降）
**问题**：LLM 纠正了数据但没有明确否定错误前提
**TODO**：
- [ ] 分析 Q50/Q51/Q54 的具体失败案例
- [ ] 在 prompt 中增加更明确的"前提否定"指令
- [ ] 考虑在检索层增加"反面证据"检索（主动找与问题前提矛盾的数据）
- [ ] 目标：H类提升到 2.0+/3

### 2. I类（拒答边界）改进
**当前状态**：2.00/3（66.7%）
**问题**：仍然会"用错误年份数据回答"（如问 2025 回答 2024）
**TODO**：
- [ ] 在检索层增加年份验证（query 的年份必须匹配 chunk 的年份）
- [ ] 在 prompt 中增加"年份不匹配时必须拒答"的规则
- [ ] 目标：I类提升到 2.5+/3

### 3. C类（跨文档对比）继续优化
**当前状态**：1.88/3（从 1.50 提升 25%）
**问题**：检索到的 chunks 年份错位
**TODO**：
- [ ] 实现精确的年份匹配过滤（query 说"2025年"只返回 2025 年的 chunks）
- [ ] 优化多文档综合的 prompt 策略
- [ ] 目标：C类提升到 2.3+/3

## 中优先级（提升系统质量）

### 4. 代码清理
- [ ] 移除未使用的 `sub_dependencies` 代码（或实现依赖感知检索）
- [ ] 清理 `complexity` 分类的使用（或删除）
- [ ] 决定是否保留证据整理层（当前禁用了）
- [ ] 整理 retrieval.py 的多种检索策略，统一入口

### 5. 评测系统改进
- [ ] 自动化回归测试（每次改动后自动跑评测）
- [ ] 评测结果版本化管理（Git 跟踪评测结果变化）
- [ ] 增加评测可视化（对比不同版本的得分变化）

### 6. 性能优化
- [ ] 权威表格补充检索增加了额外的 BM25 查询，评估性能影响
- [ ] 考虑缓存常用 query 的检索结果
- [ ] 优化 rerank 的调用频率

## 低优先级（锦上添花）

### 7. 新功能
- [ ] 支持多轮对话的上下文感知检索
- [ ] 支持图片/表格的结构化理解
- [ ] 支持用户反馈（标记回答质量）用于优化

### 8. 文档完善
- [ ] 更新 CLAUDE.md 的架构说明（反映新的检索策略）
- [ ] 编写检索层的技术文档
- [ ] 编写评测系统的使用指南

## 技术债务

### 已知的代码问题
1. **retrieval.py 复杂度高**：多种检索策略（hybrid、two-stage、supplement）混在一起，难以维护
2. **prompt.py 脆弱**：小的改动可能导致 LLM 行为剧变（如全部拒答）
3. **evidence.py 未充分使用**：建好了基础设施但没有在最终流程中发挥作用

### 潜在的架构改进
1. **检索策略路由**：根据 query 类型自动选择最优的检索策略
2. **Prompt 模板系统**：将 prompt 拆分成可组合的模块，降低改动风险
3. **评测驱动开发**：建立自动化评测流程，每次改动都有量化指标

## 下一步行动

**立即执行**（本周）：
1. 修复 H 类的 prompt 问题（明确否定错误前提）
2. 实现年份匹配验证（解决 I 类的"用错误年份回答"问题）
3. 重新跑全量评测，确认改进效果

**短期计划**（本月）：
1. C类跨文档对比优化到 2.3+/3
2. 整体分数提升到 75%+
3. 清理技术债务（移除未使用代码）

**长期目标**（下季度）：
1. 整体分数达到 80%+
2. 建立完善的评测和回归测试体系
3. 检索层架构重构（统一策略路由）
